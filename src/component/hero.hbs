     {{!-- <div>
        {{#with data}}
            <h1 class="hero_title">{{title}}</h1>
            <p class="hero_text">{{text}}</p>
        {{/with}}
        </div> --}}
            <div>
            <h1 class="hero_title">{{title}}</h1>
            <p class="hero_text">{{text}}</p>
            </div>