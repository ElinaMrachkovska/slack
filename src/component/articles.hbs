{{#each articles}}
    <div class="lorem">
      <img src="{{photo}}" id="{{id}}">
      <h2 class="card_title">{{name}}</h2>
      <p class="card_text">{{description}}</p>
      <span>Read All</span>
    </div>
    {{/each}}